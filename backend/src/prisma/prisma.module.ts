// This module is no longer used. SpacetimeModule (global) replaces PrismaModule.
// Kept as an empty placeholder to avoid breaking any imports that may reference this path.
import { Module } from '@nestjs/common';

@Module({})
export class PrismaModule {}

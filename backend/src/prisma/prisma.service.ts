// This service is no longer used. SpacetimeService replaces PrismaService.
// Kept as a placeholder to avoid import resolution errors during build transition.
import { Injectable } from '@nestjs/common';

@Injectable()
export class PrismaService {}
